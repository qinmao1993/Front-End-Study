#include <stdio.h>
#include <string.h>

int main(void)
{
    printf("hello world!");

    // 案例1
    // float weight, value;
    // printf("请输入你的体重多少斤:");
    // scanf("%f", &weight);
    // value = weight * 0.5;
    // printf("你的体重为:%.2f 千克 \n", value); // %.2f 保留2位小数

    // int arr[4] = {1, 2, 3, 4};
    // int *p2, *arr2;
    // int *p = arr;
    // p2 = p + 2;
    // arr2 = &arr[2];
    // printf("p+2的指针%p\n", p2);
    // printf("arr[2]的指针%p\n", arr2);

    // // 字符串相关
    // // char s[] = "Hello, world!";
    // // printf("The string is %zd characters long.\n", strlen(s));
    // getchar();

    return 0;
}
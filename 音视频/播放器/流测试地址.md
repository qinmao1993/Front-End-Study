# 公开测试流地址（非真实监控）

## RTSP 测试流
```bash
    # 公共测试服务器（可能不稳定）
    rtsp://wowzaec2demo.streamlock.net/vod/mp4:BigBuckBunny_115k.mov
    rtsp://184.72.239.149/vod/mp4:BigBuckBunny_115k.mov

    # 动物监控（虚拟）
    rtsp://rtsp.stream/pattern
    rtsp://rtsp.stream/animal
```

## HLS 测试直播流
```bash
    # Mux 测试流
    https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8

    # Apple 测试流
    https://devstreaming-cdn.apple.com/videos/streaming/examples/img_bipbop_adv_example_fmp4/master.m3u8

    # Akamai 测试流
    https://cph-p2p-msl.akamaized.net/hls/live/2000341/test/master.m3u8
```

## 本地模拟监控环境搭建
> 使用 FFmpeg 创建模拟 RTSP 流
```bash
    # 1. 安装 FFmpeg
    # Ubuntu/Debian: sudo apt install ffmpeg
    # macOS: brew install ffmpeg
    # Windows: 从官网下载

    # 2. 使用测试视频创建 RTSP 服务器
    ffmpeg -re -stream_loop -1 -i test_video.mp4 \
    -c:v libx264 -preset ultrafast -tune zerolatency \
    -f rtsp rtsp://localhost:8554/live/stream

    # 3. 播放地址
    # rtsp://localhost:8554/live/stream
```

## Node.js + FFmpeg 转换示例
> RTSP 转 HLS/WebRTC 架构
```text
    监控摄像头 (RTSP)
        ↓
    [ 中转服务器 ] ← FFmpeg/Nginx
        ↓
    HLS流 (m3u8) 或 WebRTC
        ↓
    Video.js 播放器
```
```js
const { spawn } = require('child_process');
const express = require('express');
const app = express();

// RTSP转HLS
function rtspToHLS(rtspUrl, outputPath) {
  const ffmpeg = spawn('ffmpeg', [
    '-rtsp_transport', 'tcp',
    '-i', rtspUrl,
    '-c:v', 'libx264',
    '-preset', 'ultrafast',
    '-tune', 'zerolatency',
    '-c:a', 'aac',
    '-f', 'hls',
    '-hls_time', '2',
    '-hls_list_size', '5',
    '-hls_flags', 'delete_segments',
    '-hls_allow_cache', '0',
    `${outputPath}/stream.m3u8`
  ]);
  
  return ffmpeg;
}

// 启动转换
const stream = rtspToHLS('rtsp://your-camera-ip:554/stream', './public/live');

app.use(express.static('public'));

app.get('/stream', (req, res) => {
  // 返回HLS流地址
  res.json({ url: '/live/stream.m3u8' });
});

app.listen(3000, () => {
  console.log('服务器运行在 http://localhost:3000');
});
```